# 一帧成片（FrameCut）项目现状总结

> 本文档用于让另一个AI快速理解项目，给出优化建议。

---

## 一、项目是什么

一个**本地运行、AI驱动**的视频创作工具，核心场景：
1. **电影解说视频生成**：上传电影正片 + 输入剧情/片名 → AI写解说稿 → 自动匹配画面 → 配音+字幕 → 出片
2. **智能卡点剪辑**：上传视频+音乐 → 自动检测场景切换 → 对齐音乐鼓点 → 出片
3. **手动精调**：PR式时间轴编辑器，可裁剪、拖动、对齐配音

技术特点：**全本地运行**，AI模型用Ollama本地部署，不花API钱。

---

## 二、技术栈

| 层 | 技术 |
|---|---|
| 后端 | Python 3.14，单文件 `webui_server.py`（约11500行），http.server |
| 前端 | 原生HTML/CSS/JS（无框架），`static/` 下3个文件 |
| 视频处理 | ffmpeg（imageio-ffmpeg自带） |
| AI写稿 | Ollama本地LLM（qwen3:14b-q4_K_M） |
| AI视觉 | Ollama本地VLM（minicpm-v4.5:q8_0 / qwen3-vl） |
| 语音识别 | faster-whisper（distil-large-v3） |
| 配音TTS | edge-tts（默认）/ CosyVoice / ChatTTS / sherpa-onnx / 云端mimo |
| 音乐分析 | librosa（节拍检测） |
| 部署 | 本地HTTP服务 `http://127.0.0.1:8765` |

用户硬件：**16GB内存 + RTX 3060 12GB**

---

## 三、核心架构

### 后端单文件结构（webui_server.py）
```
~4000行: 工具函数（ffmpeg封装、缓存、AI配置）
~5000行: AI功能（VLM描述、LLM写稿、Whisper ASR、TTS）
~7000行: 视频合成（卡点、解说、字幕烧录、配乐）
~9000行: 工作流调度（dispatch_* 系列函数）
~10000行: HTTP Handler（路由、文件上传、静态文件）
~11500行: 路由表（POST_EXACT/GET_EXACT字典）
```

### 电影解说主流程（narrate_movie → _narrate_by_plot）
```
1. 探测片长
2. ASR台词识别（whisper）→ 有缓存
3. VLM画面索引（均匀抽样+场景描述）→ 有缓存（刚加的）
4. LLM写完整解说稿（hook + beats + outro）
5. LLM语义对齐（解说词→场景）
6. 剧情理解润色（用画面+台词润色解说词）
7. 按解说词字数分配画面区间
8. TTS逐段配音
9. 视频裁剪+混音+烧字幕+配乐 → 成片
```

### 两步走模式（用户要求的）
- Phase 1：生成解说稿+所有配音 → 暂停，用户在手动调整页微调
- Phase 2：用户确认后 → 裁剪视频+合成

---

## 四、已完成的功能（按时间倒序）

### 最近完成（这几轮迭代）
1. ✅ **增量缓存**：ASR+VLM结果按视频哈希缓存，同视频换文案从40分钟→3分钟
2. ✅ **AI候选片段推荐增强**：ASR台词匹配 + VLM画面理解匹配，双维度推荐3个最佳片段
3. ✅ **时间轴NaN bug修复**：拖动片段后全部缩成0的问题
4. ✅ **TTS标记清理**：所有引擎都清理`{停顿}{情绪}{慢}`标记，不会念出来
5. ✅ **上传假死修复**：分片失败时中止所有在传请求，不再249/250卡住
6. ✅ **进度条升级**：小数点精度、已用时间、ETA、处理速度
7. ✅ **TTS失败明确报错**：不再静默显示"完成"
8. ✅ **性能优化**：相邻短场景合并、有台词才跑VLM、修复重复调用
9. ✅ **5步流程引导**：开始→素材→解说→手动调整→合成
10. ✅ **PR式时间轴编辑器**：裁剪、拖动、对齐、缩放、删除片段
11. ✅ **CosyVoice集成**：更自然的本地TTS，支持声音克隆
12. ✅ **模板市场**：电影解说/科普/搞笑吐槽等完整模板
13. ✅ **音效库**：内置免费转场音效
14. ✅ **AI自动B-roll**：解说词提到"枪"自动插入枪战画面
15. ✅ **字幕样式自定义**：字号/颜色/描边/位置
16. ✅ **导出选项**：分辨率/码率选择
17. ✅ **日夜主题切换**

---

## 五、已知痛点和问题（需要优化的方向）

### 🔴 高优先级
1. **AI剧情理解能力不足**：VLM只能描述画面，不能理解场景含义。解说词有时和画面对不上。需要更强的"剧情理解层"——结合台词+画面+上下文推断这段在讲什么。
2. **处理速度慢**：1小时视频要25-40分钟。瓶颈在VLM逐帧分析（319个场景）。
3. **代码架构**：单文件11500行，维护困难。没有模块化，函数间耦合高。
4. **配音后段丢失**：有时前几段配音正常，后面就没声音了。TTS引擎不稳定。

### 🟡 中优先级
5. **断点续跑**：长视频处理到一半中断，不能从断点继续，要从头跑。
6. **撤销/重做**：时间轴操作还不支持Ctrl+Z/Ctrl+Y（代码里有_pushUndo但没做完整的undo栈）。
7. **任务队列**：只能同时跑一个任务，不能排队。
8. **手动调整页预览**：预览视频和时间轴的同步还有bug（用户反复反馈过多次）。

### 🟢 低优先级
9. **配乐自动淡入淡出**：还没做。
10. **预设保存**：常用的风格/配音/字幕组合不能一键保存复用。
11. **进度子状态**：VLM分析时只显示百分比，不显示"正在分析第X帧"。

---

## 六、关键代码位置（方便快速定位）

| 功能 | 文件:行号 |
|---|---|
| 主入口 | webui_server.py:9335 (do_GET) / do_POST |
| 电影解说调度 | webui_server.py:9488 (dispatch_movie) |
| 解说核心流程 | webui_server.py:8487 (_narrate_by_plot) |
| VLM画面索引 | webui_server.py:7678 (_vlm_sample_timeline) |
| LLM写解说稿 | webui_server.py (llm_movie_full_script) |
| TTS统一入口 | webui_server.py:5914 (local_tts_speak) |
| TTS标记清理 | webui_server.py:5904 (_strip_tts_markup) |
| 缓存系统 | webui_server.py:4050-4135 |
| 视频合成 | webui_server.py:7360 (_compose_narration_video) |
| 卡点剪辑 | webui_server.py (dispatch_beatcut) |
| 前端时间轴 | static/app.js:3430 (renderTimeline) |
| 前端手动调整 | static/app.js (renderAdjustPanel) |
| 前端流程引导 | static/app.js:1418 (STEP_CARDS / showStep) |

---

## 七、给另一个AI的建议方向

如果你要优化这个项目，建议按这个顺序：

1. **先读代码理解架构**：从 `narrate_movie` → `_narrate_by_plot` 入手，这是核心流程
2. **最有价值的优化**：剧情理解层（在VLM描述和LLM写稿之间加一层推理）
3. **工程优化**：把11500行单文件拆成模块（但要小心不要破坏现有功能）
4. **用户体验**：断点续跑 + 撤销重做，这两个对长视频处理体验提升最大
5. **稳定性**：TTS后段丢失的问题需要重点排查

---

## 八、用户偏好（重要）

- 用户是**土木工程专业**，对准确性要求高，对bug零容忍
- 用户硬件：16GB内存 + RTX 3060 12GB，所有优化要考虑这个配置
- 用户不喜欢"成为bug测试员"，每次出bug都会不满
- 用户喜欢"全权委托"模式，让AI自己判断怎么做，不要频繁确认
- 用户要求**普通用户易用 + 专业用户可细调**的平衡
- 用户明确接受：两步走配音确认、手动调整独立页面、PR式时间轴
- 用户明确不满：预览视频跳转不生效、安装静默失败、选了音色还是edge-tts、页面太乱
