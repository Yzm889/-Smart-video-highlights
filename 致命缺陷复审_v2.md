# 致命缺陷复审 v2（2026-09-05）

> **审计范围**：在 PROGRESS.md「🛡 致命缺陷专项审计」v1 基础上，重点复查本轮任务 1~4 与 5/6/7 实现引入的新面，并以实测为依据。
>
> 优先级：**P0（用户可感知功能故障 / 数据丢失）** → **P1（隐性缺陷 / 偶发）** → **P2（健壮性 / 防御性）** → **P3（体验）**。
>
> 修复原则：最小改动、不新增依赖、保持接口兼容。

---

## 已验证的 P0（数据损坏 / 功能必坏）

### P0-1：增量状态文件非原子写，崩溃即破
- **位置**：
  - `webui_server.py:8245` `tts_state.json` 写（增量 / 最终态）
  - `webui_server.py:8373` `state.json` 增量保存
  - `handler.py:563` 同类写
- **触发条件**：进程被 kill、断电、磁盘满；任何写到一半被打断的情况
- **影响**：
  - `tts_state.json` 损毁 → 用户重新进入「调整」页 → 后端 `json.load` 抛异常 → 进度丢失 / 配音对不上 / 字幕异常
  - `state.json` 损毁 → 「增量重生成」分支完全失效，每次都重头跑
- **根因**：项目**已经有 `_atomic_json_dump()`（webui_server.py:6470，AT 机制）**，但这三处没用
- **修复方案**：三处全部替换为 `_atomic_json_dump(path, _state)`
- **回归风险**：极低（接口一致，旧调用点零改动）

### P0-2：`_get_tasks` 并发迭代 PROGRESS 可能 RuntimeError
- **位置**：`handler.py:471-490` 的 `_get_tasks`，`for rid, p in _w.PROGRESS.items():`
- **触发条件**：用户正在看任务中心（每 2 秒轮询 `/api/tasks`），同时某个 runner 线程结束 → `_runner` finally → `_start_next_queued` → `_evict_finished_progress(keep=100)` → `PROGRESS.pop(k)`（仅当 done=True）
- **影响**：
  - **理论风险**：依据 Python 规范，对 dict.items() 进行迭代时若有其他线程修改 dict 键集合，`RuntimeError: dictionary changed size during iteration` 是定义性错误
  - **实测**：（200~1000 条 PROGRESS + 持续 5000 次 pop / list 循环）在 CPython 3.13/3.14 上**未复现**（dict.items() 视图与 GIL 提供了一定保护）。但**实测阴性不能否定规范阴性**，仍视为潜在 P0
- **根因**：`_get_tasks` 没有快照（`_evict_finished_progress` 自己用 `list(_w.PROGRESS.keys())` 已规避）
- **修复方案**：`for rid, p in list(_w.PROGRESS.items()):` —— 一字之差，行为正确
- **回归风险**：零

### P0-3：handler.py `_post_cancel` 在 PROGRESS 检查与队列检查之间无锁，存在「幽灵取消」
- **位置**：`handler.py:1080-1108`
- **触发条件**：
  1. 客户端发起 POST `/api/cancel`（runid=X）
  2. 执行 `if runid not in _w.PROGRESS:` 通过
  3. _runner 线程正好结束，PROGRESS[X] 被 `_evict_finished_progress` 移除
  4. 客户端得到「未知 run」错误，但任务其实已经完成
- **影响**：偶发误报
- **根因**：PROGRESS 字典的 check-then-act 无原子保护
- **修复方案**：将 PROGRESS 字典加一把专用轻锁；`_post_cancel` 在锁内「先快照拷贝、再判断」
- **回归风险**：低（增加一个细粒度锁，全局锁顺序与 ffmpeg 无关）

---

## 已验证的 P1（正确性 / 偶发）

### P1-1：`_TASK_SEM` 名额在「pop 成功 → 重新放回」极端路径下可能丢
- **位置**：`workflows.py:973-1011` `_start_next_queued`
- **触发条件**：执行流如下（极小窗口）：
  1. 队列里有任务 A
  2. `_start_next_queued` 成功 pop(A)
  3. `_TASK_SEM.acquire(blocking=False)` 失败（名额被另一个临时路径占）
  4. 重新塞回 `_TASK_QUEUE` 头部
  5. return
- **现状**：这种行为 **没有 bug**，塞回的 A 还会在下一个完成信号触发时重试
- **风险**：极端并发下，A 被塞回后**另一个调用**也来 pop，结果两个调用都 pop 到 A 的引用。这是逻辑上不可能的，因为 `_TASK_QUEUE_LOCK` 互斥。但 `_persist_queue_unlocked()` 写盘的 I/O 在锁内，短窗口内累积会拖慢 `_start_next_queued` 启动延迟
- **修复方案**：把持久化挪到锁外（仍是 `_persist_queue_unlocked` 但调用者负责持有锁 → 入队写入 → 释放锁）
- **回归风险**：极低

### P1-2：取消后的 PROGRESS 条目与 `_evict_finished_progress` 顺序问题
- **位置**：`handler.py:_post_cancel` + `handler.py:_evict_finished_progress`
- **触发条件**：取消排队中某任务 → 标 done=True → 多次轮询触发 → 该条目被 `_evict_finished_progress` 淘汰
- **影响**：极小（用户本来就不会再去看这个 done 的卡片）
- **修复方案**：无需
- **回归风险**：N/A

---

## 代码与运行时问题（健壮性）

### P2-1：`/api/tasks` 暴露给前端的状态字段有限，缺 `queued_at` 和 `start_time`
- **位置**：`handler.py:476-486`
- **影响**：前端想按时间排序「开始执行时间」无依据，目前只能用 runid 排序
- **修复方案**：把 `start_time`、`queued_at`、`mode` 一起暴露给前端（零成本）

### P2-2：取消按钮的二次确认信息不区分队列 / 运行两类
- **位置**：`static/app.js:1617-1622` `cancelTask`
- **影响**：体验微小——用户取消队列中的任务也是「确定取消任务 X？」，但语义应是「将从队列移除」
- **修复方案**：参考 `t.queued` 切换提示文案
- **回归风险**：零

### P2-3：TASK_QUEUE_FILE 是 import-time 快照，外部代码拿到的可能是旧路径
- **位置**：`webui_server.py:236`
- **触发条件**：外部代码读 `webui_server.TASK_QUEUE_FILE` 之前如果有人改了 `OUTDIR`（实际生产不会，测试会）
- **修复方案**：直接外部暴露函数 `_task_queue_path()`，删除 `TASK_QUEUE_FILE` 常量；或保留并加 docstring 注明「仅首次 import 时有效」
- **回归风险**：零（仅文档）

---

## 已确认 **不存在** 的问题

- ✅ `ffmpeg_utils.ffmpeg_run` 整体超时、子进程回收、句柄释放：通过 wait() + 2 线程 join + stream.close + kill 后 wait
- ✅ `_atomic_json_dump` 实现正确：tempfile.mkstemp + os.fsync + os.replace（Windows 同卷原子）
- ✅ `_evict_finished_progress` 自己用 `list(_w.PROGRESS.keys())` 规避了迭代崩溃
- ✅ RUN_PROCS 的读写都通过 `_PROC_LOCK`
- ✅ `_TASK_QUEUE` 所有变更都通过 `_TASK_QUEUE_LOCK`
- ✅ TTS 测试 / Ollama / Whisper 模型文件路径解析都用 `_safe_join`

---

## 不在本次审计范围

- ❌ `webui_output` 中间产物清理策略（数据保留 / 容量 / 风控）：需业务决策
- ❌ SSRF / CSRF / XSS 实际严重度：当前默认监听 127.0.0.1，未跨网络暴露
- ❌ 1 小时视频解说性能：需要用户硬件规模数据
- ❌ module split（Task 7）：属工程化重构而非缺陷

---

## 实测依据汇总（运行结果）

### 任务 3（本次新加）回归
- `tests/test_task_queue.py`：16/16 PASS
- `tests/test_vlm_resume.py` + `tests/test_module_split.py` + `tests/test_run_cleanup.py`：33/33 PASS（不因本轮改动受影响）
- `py_compile` on `webui_server.py / handler.py / workflows.py`：OK

### 复审抽样
- `grep` 找出 6 处 `json.dump(*, open(..., 'w', encoding='utf-8'))`，其中 3 处于关键状态文件
- `grep _TASK_SEM.acquire` 与 `_TASK_SEM.release` 配对核查：3 vs 3，平衡
- 合成压力测试 PROGRESS 迭代 10000 次 × 1000 项 × 2 evict 线程：未复现 RuntimeError（CPython 3.13/3.14 的 dict.items 在 GIL 下相对受保护），仍按规范处理
