# 一帧成片 FrameCut —— 项目导入提示词（直接复制给 GLM 的第一条消息）

> 用法：把下面【提示词正文】整段复制到 GLM 新会话的第一条消息。可把你已有的界面文字截图/导出内容附在最后作参考。

---

【提示词正文】

你是资深 Python 全栈工程师 + AI 短视频工具专家。接下来我会把一个本地运行的开源项目交给你做后续优化，请先完整理解它，再给我优化方案。

## 一、项目一句话
「一帧成片 FrameCut」——本地运行的 AI 短视频工具：用户拖入视频/图片 + 选一首背景音乐，系统自动做「智能强卡点」（画面切点对齐音乐鼓点）和「AI 电影解说」（理解剧情写连贯解说词），一键合成节拍短片。单人兴趣项目，Windows 本地运行，不依赖云端也能出片。

## 二、技术栈（无框架，务必保持轻量）
- 后端：Python 标准库 `http.server`（ThreadingHTTPServer），单文件 `webui_server.py`（约 4700 行），无 Flask/FastAPI
- 前端：原生 HTML/CSS/JS（`static/` 目录，index.html + style.css + app.js），无构建工具
- 媒资处理：外部 ffmpeg（经 `ffmpeg_run()` 封装调用，Windows 下 exe 路径自动探测）
- 音频分析：librosa（onset 峰值检测音乐鼓点）
- 图像/数组：PIL + numpy（抽帧信号分析、Ken Burns 图片动效）
- AI：本地 Ollama（`qwen2.5:14b` 文字大脑 + `qwen2.5-vl` 视觉大脑）；可选云端 OpenAI 兼容 API（DeepSeek/通义/智谱等，配置文件 `ai_config.json`，不进版本库）
- 语音：本地 Whisper（ASR 台词）+ Edge-TTS（配音）
- 测试：pytest，`tests/test_core.py`，当前 48 个用例全绿

## 三、目录结构
- `webui_server.py` —— 后端全部逻辑（HTTP 路由 + 业务）
- `static/` —— 前端（index.html / style.css / app.js）
- `tests/` —— pytest 测试
- `music_library/` —— 内置免费踩点曲库（mp3）
- `webui_output/` —— 生成产物（按时间戳分目录）
- `models/` —— 本地模型（whisper 等，大文件不进 git）
- `PROGRESS.md` —— 项目演进存档（记录了每一轮改了什么、为什么）
- `README.md` —— 使用文档

## 四、核心工作流（3 条主链路，都在 webui_server.py）
1. **智能强卡点**（核心）：
   - `detect_scene_cuts`：ffmpeg `select='gt(scene,T)'` 检测场景切换
   - 一次全片抽帧（`_analyze_video_frames`，fps≈4）同时算 4 类信号 → `_detect_motion_from_frames`（动作/停顿帧）、`_detect_visual_from_frames`（镜头/色调/亮度突变）
   - `detect_strong_beats`：librosa 检测鼓点，**时间窗分桶**保证强拍均匀覆盖整段音乐
   - `plan_beat_cuts`：场景/动作/视觉切点加权吸附到最近强拍，min_seg 按强度自适应 + max_cuts 段数上限 + 超量均匀抽稀
   - `_render_beatcut`：按时间线切片（**每段必须传 `start=timeline[i]`**）→ 硬切 concat demuxer 免重编码 / 转场 xfade → 配乐
2. **电影解说**：
   - `_segment_timeline`：场景切分优先分段，`_merge_segs` 合并过碎镜头到 ≤6 个剧情环节
   - Whisper ASR 提取台词 → 本地 VLM 看图 + 台词 + 主题梗概 → **整稿连贯生成**（1 行 = 1 个镜头，强提示"剧情解说不是画面描述、少升华、详略得当"）→ 自检润色一遍
   - 云端模式：DeepSeek 整体理解后分段输出
3. **一键合成**：卡点时间线 / 解说字幕+配音 / 音乐 → 出片

## 五、关键设计决策（优化时不要破坏）
- **全功能本地可跑、不强制花钱**：本地模型 / 免费模板逐级兜底
- **人机协同**：分析阶段（卡点切点、解说词）拆出独立函数，前端预览后用户可增删切点、编辑解说词再渲染，不是机器单跑
- **模型拉取优先国内加速通道**：aria2c 多线程下载 GGUF → `ollama create` 导入，失败回退官方 pull
- **前端顶部步骤导航**（①开始…⑨记录），刷新记忆上次步骤与参数（localStorage）

## 六、已踩过的坑（务必避免再犯）
- 切片忘传 `start=timeline[i]` → 每段从源视频 0 秒截取，片头 Logo 反复出现、后面内容丢失（已修）
- 强拍用"全局最强 top_k" → 强拍集中在音乐前段，视频后段踩不上鼓点（已改时间窗分桶）
- min_seg=0.8 太碎 → 118s 视频切成 70 个 1s 碎段闪屏（已加自适应 min_seg + max_cuts 抽稀，实测 32 段）
- 动作/视觉检测各抽帧一次 → 全片解码两遍（已合并一次抽帧，耗时减半）
- 后端是进程内代码，改完必须重启服务才生效（有 `_restart_server.py` 启动器）

## 七、已知边界 / 待优化方向（供你参考，不用全做）
- 长视频（>5 分钟）抽帧内存与速度
- 节拍同步引擎与强卡点引擎两套并存，可考虑统一
- 卡点时间线与解说分段未复用同一套切点（两套可能打架）
- 本地 VLM 对复杂剧情理解有限
- 云端/本地模式切换的配置体验

## 八、现在请你做的（第一步）
1. 用一段话复述你对项目的理解（让我确认你真的懂了）
2. 给出 3~5 个「高性价比优化方向」，按 收益/成本 排序，说明改动范围和风险
3. 说明你会怎么验证改动（强调不能破坏现有 48 个 pytest）

## 九、后续协作方式（协议）
- 我会按需贴 `webui_server.py` 或 `static/app.js` 的单个函数/片段给你，你给出**精确 diff 或完整可替换代码**
- 保持风格一致：单文件无新框架、Windows 兼容、中文注释、不引入新的重量级依赖
- 每次改动后必须提醒我跑 `python -m pytest tests -q` 回归，再重启服务验证
- 涉及外部可执行文件（ffmpeg）时，用现有 `ffmpeg_run()` 封装，不要直接 subprocess 裸调

（可选附件：我提供的界面文字/截图，仅用于了解 UI 文案，不作为架构依据）

【提示词正文结束】

---

### 附：界面文字参考（你刚才上传的内容，可附在提示词后）
> 一帧成片 FrameCut · AI 节拍短片 / ①开始 ②素材 ③音乐 ④卡点 ⑤解说 ⑥AI ⑦输出 ⑧合成 ⑨记录 / AI 配置：🤖真AI(LLM) 未配置 · 👁画面描述 未配置 · 🖥本地模型 已就绪 · 📷视觉理解 已就绪 · 🎙Whisper base / 🎯智能强卡点（本应用核心）：拖入一段视频（自动识别场景切换与动作停顿帧），在「背景音乐」选一首歌（或从「免费踩点音乐库」搜一首点「使用」），系统会把画面切点自动匹对到音乐大鼓点，硬切生成强卡点短片。卡点强度：柔和（少切、稳）/ 标准（推荐）/ 强力（多切、密）。
