# 一帧成片 · 成片质量升级计划

> 制定时间：2026-09-08
> 目标：让成片从「能看」变成「耐看且专业」
> 依据：逐行读完 `video_render.py`（502 行）、`ffmpeg_utils.py`、`webui_server.py:2660-2790` 编码层后的实测结论

---

## 一、先定义「成片更好」是什么

按观众感知强弱排序：

| 维度 | 观众的原话 | 决定人 |
|---|---|---|
| **听得清** | 「有杂音 / 声音忽大忽小 / 音乐盖住人声」 | 混音策略 |
| **看着爽** | 「画面糊 / 暗的地方一块块的 / 有马赛克」 | 编码参数 + 重编码次数 |
| **不别扭** | 「话说完画面还在放 / 话没说完就切了」 | 画面↔声音时长适配 |
| **够专业** | 「像那么回事」 | 响度标准 / 字幕 / 首尾处理 |

---

## 二、现状诊断：成片链路上的 3 道损耗

当前一次「电影解说」的完整加工链：

```
源片 ──①剪切(重编码)──> cut000.mp4 … cutNNN.mp4
      ──②concat -c copy──> cut.mp4
      ──③烧字幕+缩放(重编码)──> vsub.mp4
      ──④混音(-c:v copy, 音频重编码)──> final.mp4
```

**视频被有损编码了 2 次**（①③），音频被有损编码 2 次（① 的 `-b:a 160k`、④ 的 `192k`），
中间还有 `narr{i}_fit.mp3` 的 atempo 重编码。两遍 CRF 23 的累积损失约等于单遍 CRF 26–27。

下面是逐项的证据与影响。

### 🔴 Q1｜默认走 NVENC constqp，是暗部色带的元凶

`webui_server.py:2754-2756`

```python
# 现状
['-c:v','h264_nvenc','-pix_fmt','yuv420p','-preset','p4','-rc','constqp','-qp','23']
```

三个问题叠加：

1. **`constqp` 恒定 QP，码率完全不受控**。暗场、雨雾、爆炸这类高复杂度画面码率会飙到很高仍不够用，而对话特写又浪费码率 —— 该花钱的地方没花，不该花的地方乱花。
2. **`p4` 是速度/质量折中档**。RTX 3060 是第 7 代 NVENC，`p5`/`p6` 配合 lookahead 质量明显更好，代价是慢，但 NVENC 再慢也比 CPU 快得多。
3. **缺 `-tune hq`。** 这是 ffmpeg 的 nvenc 高质量优化开关（心理视觉率失真、加权预测），不开等于白用了一块 3060。

对「电影解说」这种**大量暗部 + 渐变天空**的素材，这三点是色带（banding）和块状噪声的直接来源。

### 🔴 Q2｜CPU 回退档 `-preset veryfast` 太激进

`webui_server.py:2757`

`veryfast` 相比 `medium` 压缩率低约 12–18%，且没有 `-tune film`。
`-tune film` 是专门给真人实拍/胶片素材的参数组（保护颗粒感、抑制 banding），本项目素材 100% 适用却没用。

### 🔴 Q3｜混音直接相加、无限幅 → 削波失真

`video_render.py:478`

```python
'amix=inputs=%d:normalize=0,aresample=44100,...'
```

`normalize=0` 意味着解说 + 原声 + BGM **峰值直接相加**。三者叠加时超过 1.0 就会数字削波，
听感就是「爆音、发刺、糊」。这是成片「廉价感」最主要的声音来源，且**完全可以通过一个滤镜解决**。

同时缺少统一响度标准：不同 TTS 引擎（edge / sherpa / SAPI）输出的音量本身就不一致，
成片会出现段落之间忽大忽小。

### 🟠 Q4｜BGM 不闪避解说

`video_render.py:471` — BGM 恒定 `volume=0.16`。
原声做了 ducking（`if(between(t,...))`），但 BGM 没有。

解说视频里人声清晰度是第一位的。专业做法是 **sidechain 闪避**：解说一开口 BGM 自动压低，人声停 BGM 缓升。

### 🟠 Q5｜长视频后半段没有音乐

`video_render.py:463-475` 的注释已经写明原因：

> 不要用 `-stream_loop -1` 无限循环：apad(whole_dur) 需要读到输入 EOF 才会输出，无限循环流没有 EOF，会导致 ffmpeg 永久挂起。

于是用了 `atrim + apad`。副作用是**音乐播完就静音** —— 一首 3 分钟的 BGM 配 8 分钟的成片，后 5 分钟干巴巴。

规避方式存在：用**有限次** `-stream_loop N`（N 算好），此时有 EOF，不会挂起。

### 🟠 Q6｜画面不去适配声音，只调声音速度

`video_render.py:61-79` 的 `_fit_voice` 只做两件事：

- 配音长 → `atempo` 加速（上限 1.35×，超出就 `trim` **腰斩**）
- 配音短 → 保持原速，**留白**

结果就是两种尬 fermented：话说一半画面切走；话说完画面还杵着两三秒静止。

正解是**让画面去适配声音**：用 `setpts` 对画面做 ±10~15% 的微变速，这个幅度观众察觉不到，但能完美贴合配音，且完全不用牺牲音质去加速人声。

### 🟡 Q7｜缩放用默认算法

`webui_server.py:2773` `scale=w:h` 不指定 flags → 默认 bicubic。
1080p→720p 降采样时 bicubic 会明显发糊，应当 `flags=lanczos`。

### 🟡 Q8｜配音中间态走 MP3

`video_render.py:184-186` atempo 重编码输出 `-c:a libmp3lame -q:a 4`。
TTS 源本身多为 mp3，等于二次有损。中间态应用 PCM/WAV。

### 🟡 Q9｜字幕无阴影

`_build_subtitle_style`（`video_render.py:344`）只设了 OutlineColour/Outline，没有 Shadow。
亮画面（雪地、天空、白墙）上的白字会糊成一片。

---

## 三、改进方案（按 ROI 排序）

### P0 — 纯参数改动，零结构风险，立刻见效

| # | 改什么 | 现状 → 改后 | 预期收益 |
|---|---|---|---|
| **P0-1** | 中间产物接近无损 | ①③两遍都用 `quality=23` → ①用 `-crf 12`/`-qp 14`，③才用最终质量档 | 消除累积损失，等效画质提升约 1 个 CRF 档 |
| **P0-2** | NVENC 参数升级 | `-preset p4 -rc constqp -qp 23` → `-preset p5 -tune hq -rc vbr -cq 20 -b:v 8M -maxrate 12M -bufsize 16M -rc-lookahead 32` | 暗部色带显著减少，复杂场景不再码率不足 |
| **P0-3** | CPU 档升级 | `-preset veryfast -crf 23` → `-preset medium -crf 19 -tune film` | 同码率下清晰度明显提升，保护胶片颗粒 |
| **P0-4** | 混音加限幅 + 响度归一 | `amix:normalize=0,...` → `...,alimiter=limit=0.95,loudnorm=I=-16:TP=-1.5:LRA=11` | 消除削波爆音；段落响度统一到流媒体标准 −16 LUFS |
| **P0-5** | 配音中间态改 WAV | `-c:a libmp3lame -q:a 4` → `pcm_s16le` | 去除二次有损，人声更干净 |
| **P0-6** | 缩放换 lanczos | `scale=w:h` → `scale=w:h:flags=lanczos` | 降采样不再发糊 |
| **P0-7** | 字幕加阴影 | force_style 补 `Shadow=1,BorderStyle=1,Bold=0` | 亮画面上的可读性 |

### P1 — 需要结构性改动，收益大

| # | 改什么 | 怎么做 | 收益 |
|---|---|---|---|
| **P1-1** | **合并 ①③ 为单遍编码** | 把「剪辑 concat → 烧字幕 → 缩放 → 混音」合成一条 filter_complex，用 trim/atrim 分段 + concat 滤镜，`silence` 友好的画（详见第五节） | 视频只编码 **1 次**，画质提升最大；同时省一半渲染时间 |
| **P1-2** | 画面适配声音 | 用 `setpts=(VOICE_LEN/SPAN_LEN)*PTS` 对每段做 ±15% 内微变速，替代/补充 atempo | 消除留白与腰斩，观感提升最明显的一项 |
| **P1-3** | BGM 侧链闪避 | `[bgm][vo]sidechaincompress=threshold=0.03:ratio=8:attack=20:release=250[bgmduck]` | 人声清晰度质的飞跃 |
| **P1-4** | BGM 有限次循环 | `-stream_loop N`（N=ceil(vdur/music_dur)）+ atrim 精确截断 | 长视频全程有音乐 |
| **P1-5** | VFR→CFR 统一 | 入口 `fps=fps=30` 归一化 | 消除手机素材的音画漂移 |

### P2 — 进阶

| # | 内容 | 说明 |
|---|---|---|
| P2-1 | 10-bit 中间管线（可选开关） | `yuv420p10le`/`p010le` 彻底消除渐变色带；默认关以保证上传兼容性 |
| P2-2 | 转场与呼吸感 | 高置信度切点保留硬切，段落边界加 200ms 交叉淡化 | 【✅ 已完成 2026-09-08】 |
| P2-3 | 片头 hook / 片尾卡片 | 前 1.5s 强化 + 结尾信息卡 |
| P2-4 | 画质档位 UI | 「速度优先 / 均衡 / 画质优先」三档，映射到不同 preset+crf 组合 | 【✅ 已完成 2026-09-08】 |

---

## 四、建议的三档配置表

| 档位 | 视频参数 | 音频 | 适用 |
|---|---|---|---|
| **速度优先** | NVENC p6 + vbr cq 22，`-b:v 6M` | alimiter + loudnorm | 粗剪预览、多次迭代 |
| **均衡（推荐默认）** | NVENC **p5 + tune hq + vbr cq 20**（GPU）/ libx264 **medium crf 19 tune film**（CPU） | alimiter + loudnorm + **sidechain ducking** | 日常出片 |
| **画质优先** | 单遍管线 + libx264 **slow crf 17 tune film**（或 NVENC cq 18） | 全链路 + 手动复核 | 正式发布版本 |

---

## 五、执行顺序（每步独立可验证、可回滚）

```
阶段一（改动集中在 2 个函数，30 分钟内）
  ├ S1  video_encode_args() 参数升级            ← P0-2 P0-3
  ├ S2  中间 CRUD 产物改接近无损                  ← P0-1
  ├ S3  _compose_narration_video 音频链加限幅+响度 ← P0-4
  └ S4  scale flags=lanczos + 字幕阴影           ← P0-6 P0-7
  验证：同一段素材 before/after 对照 + 文件大小对比

阶段二（结构改动，收益最大）
  ├ S5  合并为单遍滤镜图（视频只编码 1 次）         ← P1-1  ★核心【✅ 已完成 2026-09-08】
  ├ S6  setpts 画面微变速适配声音                 ← P1-2  ★体感最强【✅ 已完成 2026-09-08】
  └ S7  BGM 侧链闪避 + 有限次循环                 ← P1-3 P1-4【✅ 已完成 2026-09-08】
  验证：真实 ffmpeg 验证三条滤镜图（setpts 时长精确 / stream_loop 0.2s 不挂起 / 侧链压低 7.5dB）
        + test_narrate_cut.py(7) 与 test_core 相关用例(19) 全绿。

> **P1-1 完成说明（2026-09-08）**：单遍合并已落地。`_cut_and_burn_video` 把「剪切 + setpts 画面微变速 + 烧字幕 + 缩放」
> 合成**一条 filter_complex、视频只编码 1 次**（直接以最终画质编码，省掉原 interim 中间重编码）。
> 新时间轴 new_spans 在编码前按 P1-2 公式解析算出，与两遍管线 piece_durs 完全一致；编码后再用 ffprobe 校验
> 实际时长做 ≤0.5s 缩放补偿（沿用两遍管线的校正逻辑）。全片连续覆盖时不裁剪仅烧字幕+缩放，行为与两遍管线一致。
> 自动回退：单遍合并抛任何异常即回退到原两遍管线（`_cut_video_by_spans` + `_compose_narration_video`），
> 由模块开关 `_SINGLE_PASS_CUT`（环境变量 `FRAMECUT_SINGLE_PASS=0` 可强制关）控制。验证见 `_verify_p1_1.py`（4 场景全过）。

> **P2-2 完成说明（2026-09-08）**：段落边界交叉淡化落在单遍管线 `_cut_and_burn_video` 内。
> 开关：`params['transition']=='crossfade'` 时各段间用 `xfade`(视频)/`acrossfade`(音频) 做 200ms 交叉淡化；
> 默认 `'hard'` 走原 `concat` 硬切，**零影响**。`use_xfade` 仅在「显式开启 且 段数≥2 且 每段≥0.6s」时生效，
> 过短片自动回退硬切（避免淡入淡出闪烁/错位）。交叉淡化令时间轴整体压缩 `(n-1)*0.2s`，`new_spans` 同步压缩，
> `_render_narrate` 会把 `segs` 接管为压缩后的 `new_spans`，配音与字幕窗口自动跟随（不漂移）。
> 前端 `index.html#exportTransition` 选择器 + `app.js` 在 `/api/narrate`、`/api/movie_compose` 注入 `transition`。
> 验证见 `_verify_p2_2.py`（5 场景全过：有/无音频交叉淡化、硬切默认、极短片回退、三段链式）。
> **顺带修复 P2-4 遗留 bug**：`_cut_and_burn_video` 在 P2-4 只改了 `video_encode_args(tier=...)` 调用却未定义 `tier` 变量，
> 导致单遍路径每次真实渲染都抛 `NameError` 静默回退两遍管线（qualityTier 与 P1-1 单遍收益实际失效）；本次补上 `tier = params.get('qualityTier')`，单遍+档位正式生效。

阶段三（打磨）
  └ S8  画质档位 UI / 转场 / 10-bit 可选（P2-4✅ P2-2✅；P2-1/P2-3 待开始）          ← P2
```

**S5 的注意点**：单遍合并后 `-c:v copy` 的机会没有了（必须重编码一次），但因为从「2 次」降到「1 次」，
净收益仍然是画质更好 + 更快。滤镜图要用 `[0:v]trim=start=..:end=..,setpts=PTS-STARTPTS[seg0]` 串联，
比现在的「每段一个 ffmpeg 进程」更省 I/O。

---

## 六、怎么验证改进真的有效（不接受「感觉变好了」）

| 指标 | 工具 | 目标 |
|---|---|---|
| **画质** | ffmpeg `libvmaf`（需完整 ffmpeg 版，imageio 自带版可能不含） | 对比源片 VMAF ≥ 93（均衡档） |
| **削波** | `ffmpeg -af astats` 检查 `Peak_level dB` | ≤ −1.0 dBFS（绝不触 0） |
| **响度** | `ffmpeg -af ebur128` | 综合响度 −16 ± 1 LUFS，LRA ≤ 11 |
| **人声清晰度** | BGM 段 vs 解说段做 `asilenceremove` 能量对比 | 解说段 SIR 提升 ≥ 6 dB |
| **音画同步** | 逐段 `atrim` 边界与实际配音长度比对 | 偏差 < 80 ms |
| **色带** | 暗场帧截图目视 + 直方图检查 | 暗部无明显台阶 |

> 验证脚本建议固化成 `tests/test_render_quality.py`，每次改动后跑一遍，防回归。

---

## 七、优先级建议

如果只做三件事，**按这个顺序**：

1. **P0-4（限幅 + 响度归一）** —— 一个滤镜，解决最容易被人听出来的「糊/爆音」，零风险
2. **P0-1 + P0-2（单次高质量编码 + NVENC 参数）** —— 解决最容易被人看出来的「暗部色块」
3. **P1-2（画面适配声音）** —— 解决最影响观感的「话说完画面还在放」

前两项都是纯参数调整，改完立刻能用业务员素材对比。第三项需要动 `_render_narrate` 的时间轴逻辑，建议单独一个 commit 并重点回归字幕对齐。

---

## 八、风险与回退

| 风险 | 应对 |
|---|---|
| NVENC 新参数在 3060 上不支持某选项 | 启动时用现有 `_probe_nvenc()` 机制扩展，失败自动回退 CPU 档 |
| loudnorm 单遍模式对短片段处理不理想 | 短于 3 秒的片段跳过 loudnorm，只用 alimiter |
| 单遍合并滤镜图出 bug | 保留现有两遍管线作为 fallback（开关控制），出问题一键切回 |
| 微变速导致 Lt 音频错位 | 限制在 ±15% 且对新时间轴做 ffprobe 校验（现有 `_cut_video_by_spans` 已有时长偏差校正逻辑可复用） |
| 渲染变慢（medium 比 veryfast 慢） | 只让**最后一刀**用高质量，中间环节用 ultrafast，整体耗时基本不变 |
