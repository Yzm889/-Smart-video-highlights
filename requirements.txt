# 一帧成片 · FrameCut - Python 依赖
# 安装: pip install -r requirements.txt
# 注意: torch 请根据 CUDA 版本单独安装，见下方说明

# === 核心依赖 ===
numpy>=1.24.0
Pillow>=9.0.0
psutil>=5.9.0
soundfile>=0.12.0
imageio-ffmpeg>=0.4.8

# === AI 模型 ===
torch>=2.0.0
torchaudio>=2.0.0
# torch 安装命令（CUDA 12.1）:
# pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121

faster-whisper>=1.0.0
sherpa-onnx>=1.10.0
huggingface-hub>=0.16.0

# === 音频处理 ===
librosa>=0.10.0

# === 视频下载 ===
yt-dlp>=2024.1.0

# === 可选：ChatTTS（本地配音，需额外下载模型）===
# ChatTTS  # 从源码安装: pip install git+https://github.com/2noise/ChatTTS.git

# === 可选：CosyVoice（本地配音，需单独配置venv）===
# CosyVoice 不通过 pip 安装，见 docs/cosyvoice_setup.md
